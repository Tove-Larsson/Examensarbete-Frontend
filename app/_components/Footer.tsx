const Footer = () => {
  return (
    <footer className="w-full text-center py-4 text-gray-500 border-t border-gray-700">
      <p>
        &copy; {new Date().getFullYear()} Dine & Flush
      </p>
    </footer>
  );
};

export default Footer;
